===============
   ethersim
===============

Simulates an old-style, half-duplex Ethernet link, including
features like exponential backoff and checksumming/voluntary
packet corruption.

To begin, make sure you have write access to /tmp, and type
`make`. Then, run ./ethersim to inspect the command line options,
and have fun!

